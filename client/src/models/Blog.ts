export interface Blog {
    id: string;
    title: string;
    description: string;
    url: string;
    date: Date;
}

export interface BlogFields {
    title: string;
    description: string;
    url: string;
    date: Date;
}
